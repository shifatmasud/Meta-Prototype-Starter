const agent = {
  async sendCommand(command) {
    // In a real application, this would send a command to the AI agent.
    // For now, we'll just simulate a response.
    return {
      component: 'Welcome',
    };
  },
};

export default agent;
